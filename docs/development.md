# Development Notes

## Local Prerequisites

- Node.js 22 or newer
- npm 11 or newer
- ngrok or cloudflared for live OAuth and Slack callbacks

## First Run

```bash
npm install
npm run check
```

Copy `.env.example` to `.env` before running live integrations. Fixture mode should remain available for development without external credentials.

